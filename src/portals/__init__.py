"""Production portal-onboarding control-plane helpers.

The portal subsystem stores long-lived configuration in PostgreSQL and uses the
existing browser/extraction/warehouse pipeline for asynchronous execution.
"""
