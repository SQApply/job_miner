# job_miner

Run locally:

```bash
python -m venv .venv
source .venv/bin/activate   # Windows: .venv\Scripts\activate
pip install -r requirements.txt
crawl4ai-setup
python -m src --root . launch-target --target strategic_staff_jobs